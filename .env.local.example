# Copy this file to .env.local for local development
# When the Azure Function runs locally on port 7071, set this:
VITE_API_URL=http://localhost:7071

# In production (Azure Static Web Apps) this variable must NOT be set
# so the frontend uses a relative /api/sendEmail URL (same origin)
